"""GET /anomaly — anomaly detection layers.

Stub for Phase 0. The detection logic (threshold / isolation forest / energy
spike) is wired up in Phase 3-4. See CLAUDE.md §5.3 and §6.
"""
from fastapi import APIRouter

router = APIRouter()


# Endpoint intentionally not implemented yet — Phase 3.
